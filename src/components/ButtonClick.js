export function ButtonClick() {
    console.log("The button has been clicked...")

    // Step 1 create component in components folder, Step 2 import component to page where it's being called, Step 3 Call on function on button click or event. 
}